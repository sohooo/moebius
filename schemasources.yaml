sources:
  - component: kubernetes
    version: v1
    source_type: file
    paths:
      - internal/validate/schemas/kubernetes/v1/*.json
    note: Repo-local Kubernetes schema bundle source files.

  - component: cloudnative-pg
    version: v1
    source_type: file
    paths:
      - internal/validate/schemas/platform/cloudnative-pg/v1/*.json

  - component: cilium
    version: v1
    source_type: file
    paths:
      - internal/validate/schemas/platform/cilium/v1/*.json

  - component: envoy-gateway
    version: v1
    source_type: file
    paths:
      - internal/validate/schemas/platform/envoy-gateway/v1/*.json

  - component: openbao
    version: v1
    source_type: file
    paths:
      - internal/validate/schemas/platform/openbao/v1/*.json

  - component: keycloak
    version: v1
    source_type: file
    paths:
      - internal/validate/schemas/platform/keycloak/v1/*.json

  - component: longhorn
    version: v1
    source_type: file
    paths:
      - internal/validate/schemas/platform/longhorn/v1/*.json
