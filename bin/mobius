#!/usr/bin/env bash

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
BINARY="$ROOT/møbius"

if [[ -x "$BINARY" ]]; then
  exec "$BINARY" "$@"
fi

exec go run ./cmd/mobius "$@"
